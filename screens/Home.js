import { StyleSheet, Text, View } from 'react-native';
import { useContext } from 'react';

const ChatList = (props) => {
    return (
        <Text>This is where chats will go</Text>
    )
}

export default ChatList;